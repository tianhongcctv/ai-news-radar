name: Manual Smoke Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print repository context
        run: |
          echo "Manual GitHub Actions smoke test"
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
